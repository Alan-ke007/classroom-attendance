# ============================================================
# 前端环境变量模板
# 复制本文件为 .env.development / .env.production 后填写本地值
# ⚠️ 真实 .env.* 文件已被 .gitignore 忽略，禁止提交到 git
# ============================================================

# 后端 API 基础路径：开发期走 Vite 代理填 /api，生产填后端域名(如 https://api.xxx.com/api)
VITE_API_BASE_URL=/api

# 算法服务地址：当前前端直连；后续计划改走后端代理(见安全优先级③)
VITE_ALGORITHM_BASE_URL=http://localhost:5000
